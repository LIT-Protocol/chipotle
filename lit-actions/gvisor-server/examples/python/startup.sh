# Startup script for the Python example: the sandbox only ever executes
# `bash startup.sh`; launching the language runtime is the script's job.
# Top-level js-params are already in the environment (e.g. $pkpId).
python3 main.py
