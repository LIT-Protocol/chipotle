# Startup script for the Python example: `lit run` (like the sandbox) only
# ever executes `bash startup.sh`; launching the language runtime is the
# script's job. Top-level js-params are already in the environment.
python3 main.py
