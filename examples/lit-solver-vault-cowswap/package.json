{
  "name": "lit-solver-vault-cowswap",
  "version": "0.0.1",
  "private": true,
  "description": "Lit Action + vault example: policy-gated key custody for a CoW Protocol solver. The settlement-authorizing key lives in a Lit Action, not on the solver's box. Tier 1: runs against a self-deployed GPv2Settlement on Base Sepolia.",
  "scripts": {
    "compile": "hardhat compile",
    "setup": "node scripts/setup-cow.js",
    "order": "node scripts/order.js",
    "solve": "node scripts/solve.js",
    "attack": "node scripts/attack.js"
  },
  "dependencies": {
    "@cowprotocol/contracts": "^1.6.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.22.10"
  }
}
