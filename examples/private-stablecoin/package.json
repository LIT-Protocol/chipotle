{
  "name": "private-stablecoin",
  "version": "0.0.1",
  "private": true,
  "description": "Lit Action + ERC-20 example: a compliant private stablecoin. Balances and transfers are hidden on-chain (commitment/nullifier notes); the Lit Action is the prover that replaces a ZK circuit, with OFAC screening baked in and selective regulator disclosure.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "setup": "node scripts/setup.js",
    "deploy:baseSepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "demo": "node scripts/demo.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.22.10"
  }
}
