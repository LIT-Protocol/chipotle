{
  "name": "prediction-market-oracle",
  "version": "0.0.1",
  "private": true,
  "description": "Lit Action that resolves prediction-market questions via AI consensus across Perplexity, OpenAI, and Anthropic.",
  "scripts": {
    "compile": "hardhat compile",
    "setup": "node scripts/setup.js",
    "deploy:baseSepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "propose": "node scripts/propose.js",
    "resolve": "node scripts/resolve.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.22.10"
  }
}
