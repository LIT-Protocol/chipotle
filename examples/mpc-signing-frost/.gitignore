node_modules/
.env
# The user's share(s) — never commit them.
.mpc-store.json
.mpc-cold-share.json
# Generated by `npm run build:action` (wasm glue inlined) — rebuild locally.
action/mpcSigner.bundled.js
